# Pinned dependencies for update_playlist_saves.py (Update Spotify Playlist Saves workflow).
# Versions match what pip resolves on Python 3.9 for the currently-passing runs.
requests==2.32.5
pytz==2026.2
